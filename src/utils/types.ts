export enum TransactionType {
  Credit = 'CREDIT',
  Debit = 'DEBIT',
}

export enum TransactionStatus {
  Pending = 'PENDING',
  Expired = 'EXPIRED',
  Paid = 'PAID',
}

export enum TransactionCategory {
  Food = 'FOOD',
}
